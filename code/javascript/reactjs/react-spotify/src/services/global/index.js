import * as Selectors from './selectors';
import Reducers, { Creators } from './store';

export const globalSelectors = Selectors;
export const globalCreators = Creators;
export const globalReducers = Reducers;
